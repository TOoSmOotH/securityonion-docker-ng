input {
  beats {
    port => "5044"
    tags => [ "beat" ]
  }
}
filter {
  if "ids" in [tags] {
    mutate {
      remove_tag => ["beat"]
    }
  }

  if "bro" in [tags] {
    mutate {
      remove_tag => ["beat"]
    }
  }
}
